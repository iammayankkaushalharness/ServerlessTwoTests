service: randommayank
frameworkVersion: '3.30.1'

provider:
  name: aws
  runtime: java8

# package:
#   artifact: <+artifact.path>  

functions:
  currentTime1.0:
    handler: com.serverless.Handler
    # image: <+artifact.image>
    events:
      - httpApi:
          path: /time
          method: get
